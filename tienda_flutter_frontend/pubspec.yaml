name: tienda_flutter_frontend
description: A simple CRUD frontend for tienda backend in Flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  provider: ^6.0.0

flutter:
  uses-material-design: true
